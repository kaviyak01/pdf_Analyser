spring.application.name=pdf-Analyser
# Reads GEMINI_API_KEY from environment variable at runtime
# This means the key is NEVER hardcoded in your source code
gemini.api.url=https://api.groq.com/openai/v1/chat/completions
# App runs on port 8080 by default
server.port=8080